<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <RootNamespace>Pawnsmith.Cli</RootNamespace>
    <AssemblyName>pawnsmith-cli</AssemblyName>
  </PropertyGroup>

  <!-- Throwaway harness (B.7), never shipped and excluded from the Docker image.
       It gains project references when it gains a reason to; today it has none. -->

</Project>
