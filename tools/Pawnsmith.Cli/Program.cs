// B.7 — Throwaway command-line harness, not shipped and excluded from the Docker
// image. It exists only so that a sheet can be printed before the UI exists.
//
// Empty until T1 gives it a use case to call.

Console.Error.WriteLine("pawnsmith-cli: not implemented yet.");
return 1;
